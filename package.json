{
  "name": "tscr",
  "version": "2.0.2",
  "description": "",
  "scripts": {
    "start": "tsc -w",
    "build": "tsc",
    "clean": "rm -f bin/tscr.js",
    "publish:patch": "npm run clean && npm run build && npm version patch -f && npm publish"
  },
  "bin": {
    "tscr": "./bin/tscr.js"
  },
  "files": [
    "template/",
    "bin/tscr.js"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/fs-extra": "^4.0.0",
    "cross-spawn": "^5.1.0",
    "fs-extra": "^1.0.0",
    "minimist": "^1.2.0"
  },
  "devDependencies": {
    "@types/minimist": "^1.2.0",
    "@types/node": "^8.0.17",
    "typescript": "^2.4.2"
  }
}
